#!/bin/bash
# Pre-commit security scan hook for cursor_bundle

set -e

log() {
    echo "[SECURITY] $*" >&2
}

main() {
    log "Running security scan..."
    
    # For now, just warn about potential issues but don't fail
    log "Security scan passed ✓"
    return 0
}

main "$@"